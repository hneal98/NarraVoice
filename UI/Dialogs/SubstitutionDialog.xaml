<Window x:Class="NarraVoice.UI.Dialogs.SubstitutionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pronunciation Substitutions"
        Width="560"
        Height="440"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Global Pronunciation Substitutions"
                       FontSize="13" FontWeight="Bold"/>
            <TextBlock Text="These apply to all projects. Original text is replaced before rendering."
                       FontSize="11" Foreground="#555555" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Substitution list -->
        <DataGrid x:Name="SubstitutionGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="True"
                  CanUserDeleteRows="True"
                  CanUserReorderColumns="False"
                  CanUserResizeRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderBrush="#CCCCCC"
                  BorderThickness="1"
                  Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Original Text"
                                    Binding="{Binding Original, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*"/>
                <DataGridTextColumn Header="Replacement"
                                    Binding="{Binding Replacement, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Add row controls -->
        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewOriginalBox"
                     Grid.Column="0"
                     Margin="0,0,4,0"
                     Padding="4,3"
                     ToolTip="Original text to replace"/>
            <TextBlock Grid.Column="1"
                       Text="→"
                       VerticalAlignment="Center"
                       Margin="0,0,4,0"/>
            <TextBox x:Name="NewReplacementBox"
                     Grid.Column="2"
                     Margin="0,0,4,0"
                     Padding="4,3"
                     ToolTip="Replacement text"/>
            <Button Grid.Column="3"
                    Content="Add"
                    Width="60"
                    Click="OnAddClick"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Save"
                    Width="80"
                    Margin="0,0,8,0"
                    Click="OnSaveClick"
                    IsDefault="True"/>
            <Button Content="Cancel"
                    Width="80"
                    Click="OnCancelClick"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

